path: ./ImageDetermine/Test_Alpha/dataset  # データセットのルートパス
train: 
  - train/images  # 訓練用画像のパス
  - train/annotations/train_annotation.json  # 訓練用アノテーションファイルのパス
val:
  - valid/images  # 検証用画像のパス
  - valid/annotations/valid_annotation.json  # 検証用アノテーションファイルのパス

nc: 3  # クラス数
names: ['Fameu', 'Neiloah', 'Santu']  # クラス名